name: firebase_issue_repro
description: A minimal repro of firebase issue 177 (https://github.com/firebase/firebase-dart/issues/177).

environment:
  sdk: '>=2.0.0-dev.55.0 <2.0.0'

dependencies:
  firebase: 4.5.0

dev_dependencies:
  build_runner: 0.8.10
  build_web_compilers: 0.4.0+4